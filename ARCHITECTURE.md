# Architecture

Each ASI loads independently. Only the host links the native hook backend;
plugins use the public, versioned SDK package.

## Components

| Area | Location | Purpose |
| --- | --- | --- |
| Bootstrap and ABI | `src/lib.rs`, `src/host_api/mod.rs`, `src/host_api/animations.rs`, `src/host_api/chat_commands.rs`, `src/host_api/chat_input.rs`, `src/host_api/commands.rs`, `src/host_api/connection.rs`, `src/host_api/conversions.rs`, `src/host_api/dialog.rs`, `src/host_api/environment.rs`, `src/host_api/events.rs`, `src/host_api/handles.rs`, `src/host_api/local_commands.rs`, `src/host_api/local_state.rs`, `src/host_api/messages.rs`, `src/host_api/network.rs`, `src/host_api/player_commands.rs`, `src/host_api/players.rs`, `src/host_api/pools.rs`, `src/host_api/raw.rs`, `src/host_api/sampfuncs.rs`, `src/host_api/snapshots.rs`, `src/host_api/textdraws.rs`, `src/host_api/text_labels.rs`, `src/logging.rs` | Start outside `DllMain`, publish status, poll and synchronize unit and typed command receipts, convert owned snapshots into fixed ABI output storage, operate on callback-local bitstreams and native string codecs, read SAMP game state, server metadata, version, cached chat input and command-name snapshots, connection lifecycle commands, local-player actions, attributes, names, display-message, and textdraw create/update/delete commands, queue local chat-input, local UI, text-label, and dialog commands, allocate native text-label IDs and create native textdraws in explicit slots on the game thread, recreate label text, register native chat commands through fixed trampolines with callback-safe subscription lifetime, read and resolve cached local animations, expose local UI state, the active-dialog core, detailed dialog snapshots, and one-shot owned client-side dialog-close responses, perform forward/reverse handle lookups, serve local-player snapshots, player-pool scalars, pool existence, and pooled-record snapshots, send or emulate packet/RPC traffic, optionally detect SAMPFUNCS and invoke its own console logger, expose validated opaque native addresses, and export `SampClientSdkApiV1`. |
| Runtime | `src/runtime.rs`, `src/runtime/errors.rs`, `src/runtime/options.rs`, `src/event.rs`, `src/bitstream.rs` | Own bounded event data, isolate runtime failures and send policy, dispatch listeners, and send exact RakNet payloads. |
| Native backend | `src/platform/win32/mod.rs`, `src/platform/win32/chat_entries.rs`, `src/platform/win32/commands.rs`, `src/platform/win32/requests.rs`, `src/platform/win32/reads.rs`, `src/platform/win32/gangzones.rs`, `src/platform/win32/handles.rs`, `src/platform/win32/hooks.rs`, `src/platform/win32/objects.rs`, `src/platform/win32/packets.rs`, `src/platform/win32/players.rs`, `src/platform/win32/sampfuncs.rs`, `src/platform/win32/text_labels.rs`, `src/platform/win32/textdraws.rs`, `src/platform/win32/vehicles.rs`, `src/platform/win32/r1_client.rs`, `src/platform/win32/r1_client/addresses.rs`, `src/platform/win32/r1_client/memory.rs`, `src/client.rs` | Capture immutable attach-time configuration separately from mutable backend state; select networking offsets, own hook lifecycle and tick execution, keep native detours in `hooks.rs`, log each enabled named MinHook target with its detour and trampoline, validate and dispatch native bitstreams/raw packets through listeners, perform native outbound sends and incoming packet/RPC emulation, bound producer-side game/cache requests, report public lock contention as retryable `Busy` without blocking the game tick, expose published scalar, snapshot, animation-catalog, chat-history, gangzone, handle-cache, object, player, owned on-foot, in-car, passenger, and trailer sync, text-label, textdraw, and vehicle reads, optionally retain SAMPFUNCS's exported console logger, isolate approved R1/GTA addresses and native layouts, and validate every direct access through guarded memory helpers. |
| Public SDK | `sdk/src/lib.rs`, `sdk/src/host_api/animations.rs`, `sdk/src/host_api/chat_input.rs`, `sdk/src/host_api/connection.rs`, `sdk/src/host_api/dialog.rs`, `sdk/src/host_api/environment.rs`, `sdk/src/host_api/events.rs`, `sdk/src/host_api/handles.rs`, `sdk/src/host_api/local_commands.rs`, `sdk/src/host_api/local_state.rs`, `sdk/src/host_api/messages.rs`, `sdk/src/host_api/network.rs`, `sdk/src/host_api/player_commands.rs`, `sdk/src/host_api/players.rs`, `sdk/src/host_api/pools.rs`, `sdk/src/host_api/raw.rs`, `sdk/src/host_api/sampfuncs.rs`, `sdk/src/host_api/status.rs`, `sdk/src/host_api/text_labels.rs`, `sdk/src/host_api/textdraws.rs`, `sdk/src/limits.rs`, `sdk/src/facade/mod.rs`, `sdk/src/facade/local_player.rs`, `sdk/src/facade/network.rs`, `sdk/src/facade/pools.rs`, `sdk/src/facade/sampfuncs.rs`, `sdk/src/facade/ui.rs`, `sdk/src/raknet.rs`, `sdk/src/raw.rs` | Resolve the host through `Samp`, keep private `HostApi` wrapper groups behind the root public type, publish fixed capacities under `limits`, provide safe subsystem views, including optional loaded-SAMPFUNCS console interop, callback helpers, owned BitStreams, protocol catalogs, and explicit unsafe native-address access. |
| Typed events | `sdk/src/events/`, `sdk/src/events/rpc/outgoing/chat.rs`, `sdk/src/events/rpc/outgoing/connection.rs`, `sdk/src/events/rpc/outgoing/damage.rs`, `sdk/src/events/rpc/outgoing/object.rs`, `sdk/src/events/rpc/outgoing/session.rs`, `sdk/src/events/rpc/outgoing/ui.rs`, `sdk/src/events/rpc/outgoing/vehicle.rs` | Provide bounded R1 packet/RPC codecs, including outgoing chat, slash-command, join, damage, object-edit, session, dialog, UI-interaction, and vehicle codecs; retain unrelated fixed-layout singleton codecs in the root, and provide mock ABI support. |
| Examples | `examples/` | Show a minimal subscription and a chat-command plugin. |
| Layout fixture | `tests/fixtures/raknet_layout.cpp` | Independently verify native packing for boundary layouts. |

Leaf unit tests live with their implementation modules. Parent test modules retain mock-ABI callback behavior, native ABI/layout checks, and Win32 queue-to-pump-to-cache, invalidation, and hook-lifecycle integration behavior.

## Structural ownership

The public SDK root is a re-exporting facade: `types.rs`, `abi.rs`, `api.rs`,
`resolve.rs`, and `subscriptions.rs` own safe values, the C ABI declarations,
safe wrapper glue, host discovery, and callback ownership. Win32 keeps shared
state and tick ordering in `mod.rs`; `backend.rs`, `commands.rs`, `requests.rs`,
`refresh.rs`, `native_bitstream.rs`, and `hooks.rs` own forwarding, command
execution, refresh queues, cache publication, native string/bitstream handling,
and hook patching. The R1 bridge keeps profile validation in `r1_client.rs`
while singleton lookup, native aliases, textdraws, UI, player/pool reads, and
handle lookups are isolated in `r1_client/` child modules. Host listener
registration and callback dispatch live in `host_api/listeners.rs`, while
native chat-command callback slots and teardown synchronization live in
`host_api/chat_commands.rs`; the root retains the exported entry point and
ordered ABI table.
