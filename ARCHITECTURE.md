# Architecture

Each ASI loads independently. Only the host links the native hook backend;
plugins use the public, versioned SDK package.

## Components

| Area | Location | Purpose |
| --- | --- | --- |
| Bootstrap and ABI | `src/lib.rs`, `src/host_api/mod.rs`, `src/host_api/animations.rs`, `src/host_api/chat_commands.rs`, `src/host_api/chat_input.rs`, `src/host_api/commands.rs`, `src/host_api/connection.rs`, `src/host_api/conversions.rs`, `src/host_api/dialog.rs`, `src/host_api/environment.rs`, `src/host_api/events.rs`, `src/host_api/handles.rs`, `src/host_api/local_commands.rs`, `src/host_api/local_state.rs`, `src/host_api/messages.rs`, `src/host_api/network.rs`, `src/host_api/player_commands.rs`, `src/host_api/players.rs`, `src/host_api/pools.rs`, `src/host_api/raw.rs`, `src/host_api/sampfuncs.rs`, `src/host_api/snapshots.rs`, `src/host_api/textdraws.rs`, `src/host_api/text_labels.rs`, `src/logging.rs` | Start outside `DllMain`, publish status, poll and synchronize unit and typed command receipts, convert owned snapshots into fixed ABI output storage, operate on callback-local bitstreams and native string codecs, read SAMP game state, server metadata, version, cached chat input and command-name snapshots, connection lifecycle commands, local-player actions, attributes, names, display-message, and textdraw create/update/delete commands, queue local chat-input, local UI, text-label, and dialog commands, allocate native text-label IDs and create native textdraws in explicit slots on the game thread, recreate label text, register native chat commands through fixed trampolines with callback-safe subscription lifetime, read and resolve cached local animations, expose local UI state, the active-dialog core, detailed dialog snapshots, and one-shot owned client-side dialog-close responses, perform forward/reverse handle lookups, serve local-player snapshots, player-pool scalars, pool existence, and pooled-record snapshots, send or emulate packet/RPC traffic, optionally detect SAMPFUNCS and invoke its own console logger, expose validated opaque native addresses, and export `SampClientSdkApiV1`. |
| Runtime | `src/runtime.rs`, `src/runtime/errors.rs`, `src/runtime/options.rs`, `src/event.rs`, `src/bitstream.rs` | Own bounded event data, isolate runtime failures and send policy, dispatch listeners, and send exact RakNet payloads. |
| Protocol | `crates/samp-protocol/` | Own the platform-independent bounded Protocol `BitStream`, cursor-free exact-bit payload values, public neutral `types` and `limits`, one canonical set of cursor-preserving Wire read/write primitives, transport-neutral bit I/O contracts, and nominal built-in Packet/RPC Wire descriptors whose public identity hides private codec implementations. Public generic directional wrappers compose custom or ad-hoc messages with explicit trailing policies. Protocol also owns separate name catalogs, outgoing chat/command, profile-neutral byte-aligned RPC codecs, common byte-aligned Packet codecs, R1 exact-bit remote synchronization Packet codecs, four fixed incoming RPC batches, and the R1 player/session group from `INIT_GAME` through `UPDATE_SCORES_AND_PINGS`. Directional marker traits reject invalid typed subscriptions and sends at compile time. It has no Host, SDK FFI, Native profile, hook, or Windows dependency. |
| Native backend | `src/platform/win32/mod.rs`, `src/platform/win32/native_client/`, `src/platform/win32/chat_entries.rs`, `src/platform/win32/commands.rs`, `src/platform/win32/requests.rs`, `src/platform/win32/reads.rs`, `src/platform/win32/gangzones.rs`, `src/platform/win32/handles.rs`, `src/platform/win32/hooks.rs`, `src/platform/win32/objects.rs`, `src/platform/win32/packets.rs`, `src/platform/win32/players.rs`, `src/platform/win32/sampfuncs.rs`, `src/platform/win32/text_labels.rs`, `src/platform/win32/textdraws.rs`, `src/platform/win32/vehicles.rs`, `src/client.rs` | Capture immutable attach-time configuration separately from mutable backend state; select one immutable native-client profile for R1, R3-1, R5-1, or DL-R1; own hook lifecycle and one profile-independent tick/cache-refresh path, keep native detours in `hooks.rs`, log each enabled named MinHook target with its detour and trampoline, validate and dispatch native bitstreams/raw packets through listeners, perform native outbound sends and incoming packet/RPC emulation, bound producer-side game/cache requests, report public lock contention as retryable `Busy` without blocking the game tick, expose published scalar, snapshot, animation-catalog, chat-history, gangzone, handle-cache, object, player, owned on-foot, in-car, passenger, and trailer sync, text-label, textdraw, and vehicle reads, optionally retain SAMPFUNCS's exported console logger, isolate profile data from shared operations, and validate every direct access through guarded memory helpers. |
| Public SDK | `sdk/src/lib.rs`, `sdk/src/host_api/animations.rs`, `sdk/src/host_api/chat_input.rs`, `sdk/src/host_api/connection.rs`, `sdk/src/host_api/dialog.rs`, `sdk/src/host_api/environment.rs`, `sdk/src/host_api/events.rs`, `sdk/src/host_api/handles.rs`, `sdk/src/host_api/local_commands.rs`, `sdk/src/host_api/local_state.rs`, `sdk/src/host_api/messages.rs`, `sdk/src/host_api/network.rs`, `sdk/src/host_api/player_commands.rs`, `sdk/src/host_api/players.rs`, `sdk/src/host_api/pools.rs`, `sdk/src/host_api/raw.rs`, `sdk/src/host_api/sampfuncs.rs`, `sdk/src/host_api/status.rs`, `sdk/src/host_api/text_labels.rs`, `sdk/src/host_api/textdraws.rs`, `sdk/src/limits.rs`, `sdk/src/facade/mod.rs`, `sdk/src/facade/local_player.rs`, `sdk/src/facade/network.rs`, `sdk/src/facade/pools.rs`, `sdk/src/facade/sampfuncs.rs`, `sdk/src/facade/ui.rs`, `sdk/src/raw.rs` | Resolve the host through `Samp`, keep private `HostApi` wrapper groups behind the root public type, publish fixed capacities under `limits`, provide safe subsystem views, adapt callback reads to the Protocol bit I/O contract, and expose explicit unsafe native-address access. Protocol values are imported directly from `samp-protocol`; the SDK does not re-export them. |
| Typed events | `sdk/src/events/`, `crates/samp-protocol/src/packet/common.rs`, `crates/samp-protocol/src/packet/r1.rs`, `crates/samp-protocol/src/rpc/incoming/fixed.rs`, `crates/samp-protocol/src/rpc/incoming/fixed/phase15.rs`, `crates/samp-protocol/src/rpc/incoming/r1.rs`, `crates/samp-protocol/src/rpc/outgoing/chat.rs`, `crates/samp-protocol/src/rpc/outgoing/common.rs`, `sdk/src/events/packet/incoming.rs`, `sdk/src/events/rpc/outgoing/damage.rs`, `sdk/src/events/rpc/outgoing/object.rs` | Adapt callback-local events to directional Protocol descriptors and retain SDK callback actions. Incoming and outgoing Packet/RPC descriptor types are distinct, including when an ID has different layouts. Each Protocol built-in has a nominal semantic descriptor; private codecs may change without changing downstream descriptor types. Protocol owns common byte-aligned Packet codecs, R1 exact-bit remote synchronization Packet codecs, outgoing chat/slash-command, profile-neutral byte-aligned RPC codecs, four fixed incoming RPC batches, and the R1 player/session group. The SDK retains exact-bit damage and object-edit codecs, `SHOW_DIALOG` until Native encoded strings cross the extension boundary, and mock ABI support. |
| Examples | `examples/` | Show a minimal subscription and a chat-command plugin. |
| Layout fixture | `tests/fixtures/raknet_layout.cpp` | Independently verify native packing for boundary layouts. |

Leaf unit tests live with their implementation modules. Parent test modules retain mock-ABI callback behavior, native ABI/layout checks, and Win32 queue-to-pump-to-cache, invalidation, and hook-lifecycle integration behavior.

## Structural ownership

The public SDK root is a re-exporting facade: `types.rs`, `abi.rs`, `api.rs`,
`resolve.rs`, and `subscriptions.rs` own safe values, the C ABI declarations,
safe wrapper glue, host discovery, and callback ownership. Win32 keeps shared
state and tick ordering in `mod.rs`; `backend.rs`, `commands.rs`, `requests.rs`,
`refresh.rs`, `native_bitstream.rs`, and `hooks.rs` own forwarding, command
execution, refresh queues, cache publication, native string/bitstream handling,
and hook patching. `native_client/` selects the immutable direct profile
separately from networking offsets. One `NativeClientProfile` drives every
cache refresh and game tick for R1, R3-1, R5-1, and DL-R1. The four profile
specifications contain verified layouts and narrow strategies; shared modules
own singleton lookup, native aliases, textdraws, UI, player/pool reads, and
handle lookups. Host listener
registration and callback dispatch live in `host_api/listeners.rs`, while
native chat-command callback slots and teardown synchronization live in
`host_api/chat_commands.rs`; the root retains the exported entry point and
ordered ABI table.
