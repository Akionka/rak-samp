# Phase 0 baseline anchor — 2026-08-26

This record captures the complete working tree at the start of Phase 0. The
working tree, including its pre-existing tracked and untracked changes, is the
source of truth. The external `rak_rs.zip` archive is not used to reconstruct
this baseline.

## Capture identity

- Captured: `2026-08-26T09:06:29.1093342+03:00`
- Branch: `feature/helpers`
- HEAD: `2b64d43c76560cccf86e23ffd8818f9fdd3f5d1b`
- Repository: `C:\Development\rak_rs`

The complete porcelain status before this baseline file was created was:

```text
 M CONTEXT.md
 M TODO.md
?? docs/adr/0001-rust-first-single-host-product.md
?? docs/adr/0002-require-exact-target-identity-and-grade-b-evidence.md
?? docs/adr/0003-drain-before-callback-context-reclamation.md
?? docs/adr/0004-use-scoped-game-thread-context-for-native-access.md
?? docs/adr/0005-separate-service-discovery-from-backend-readiness.md
?? docs/adr/0006-use-current-pool-semantics-for-samp-gta-mappings.md
?? docs/rust-modding-infrastructure-handoff.md
```

The status above was captured with:

```powershell
git status --porcelain=v1 --untracked-files=all
```

## Toolchain and build configuration

| Item | Baseline value |
| --- | --- |
| Declared `rust-version` | `1.87` in `Cargo.toml` |
| Executed Rust | `rustc 1.98.0 (88d9e12ae 2026-08-18)` |
| Executed Cargo | `cargo 1.98.0 (797e8a9bc 2026-08-05)` |
| Active toolchain | `stable-x86_64-pc-windows-msvc (default)` |
| Target | `i686-pc-windows-msvc` |
| Target configuration | `.cargo/config.toml`: `[build] target = "i686-pc-windows-msvc"` |
| Makefile target configuration | `Makefile.toml`: `CARGO_BUILD_TARGET = "i686-pc-windows-msvc"` |
| Process `CARGO_TARGET_DIR` before validation | unset |

The user Cargo configuration at `C:\Users\Akionka\.cargo\config.toml`
declares `build-dir = "E:/cargo-build/"`. That path is not accessible in this
workspace environment. Validation therefore used these process-local overrides
to keep all generated build files under the repository:

```powershell
$env:CARGO_BUILD_BUILD_DIR = 'C:\Development\rak_rs\target\cargo-build'
$env:CARGO_TARGET_DIR = 'C:\Development\rak_rs\target\cargo-build'
```

## Initial validation

The canonical workspace checks were run with the two build-directory overrides
shown above. Each command was run before creating this file.

| Command | Exit | Result |
| --- | ---: | --- |
| `cargo fmt --all -- --check` | `0` | passed |
| `cargo test --workspace --all-targets --locked` | `0` | 306 passed, 0 failed, 0 ignored |
| `cargo clippy --workspace -- -D warnings` | `0` | passed |
| `cargo build --workspace --release --locked` | `0` | release build passed |

An additional `cargo clippy --workspace --all-targets --locked -- -D warnings`
run was not part of the canonical project check. It exited `1` on three
pre-existing test-only lints introduced by Rust 1.98 (`unusual_byte_groupings`
and `manual_c_str_literals`); no source changes were made for this baseline
capture.

After adding this documentation, the locked equivalent
`cargo clippy --workspace --locked -- -D warnings` also exited `0`.

## Release Host artifact

The locked release build produced this exact Host artifact:

- Path: `target/cargo-build/i686-pc-windows-msvc/release/samp_client_sdk.dll`
- Profile: `release`
- Target: `i686-pc-windows-msvc`
- SHA-256: `0CD0594B55CCD8E3289498D8EA03973DB668BF2722C519ED4BDCC7FFE5D34DA0`

The hash was captured with:

```powershell
Get-FileHash -Algorithm SHA256 target/cargo-build/i686-pc-windows-msvc/release/samp_client_sdk.dll
```

The complete PE export list was captured with `objdump -p` from
`C:\msys64\ucrt64\bin\objdump.exe`:

| Ordinal | RVA | Export |
| ---: | ---: | --- |
| 1 | `0x0006B330` | `DllMain` |
| 2 | `0x00052A60` | `SampClientSdk_GetApiV1` |

No other exports were present. The export audit reported two entries and the
image name `samp_client_sdk.dll`.

## Scope boundary

This issue adds documentation only. No production runtime behavior, ABI table,
or test code is changed by the baseline anchor.
