[package]
name = "gta-sa"
version = "0.1.0-alpha.4"
edition = "2024"
rust-version = "1.98"
description = "Plugin-side GTA San Andreas value types and safe facade"
license = "MIT"
repository = "https://github.com/Akionka/samp-client-sdk"

[lib]
name = "gta_sa"

[dependencies]
modkit-abi = { version = "=0.1.0-alpha.4", path = "../modkit-abi" }
modkit-sdk = { version = "=0.1.0-alpha.4", path = "../modkit-sdk" }
